<div id="content">
  <h2 class="pageTitle"><%= @page.title %></div>

  <div><%= render_md @page.body %></div>

  <% if current_user && current_user.admin? %>
    <%= link_to 'Edit', edit_page_path(@page) %> |
    <%= link_to 'Back', pages_path %>
  <% end %>
</div>
