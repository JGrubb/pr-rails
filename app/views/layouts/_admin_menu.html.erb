<% if admin? %>
  <div class="navbar">
    <div class="navbar-inner">
      <ul class="nav">
        <%= content_tag(:li, :class => "dropdown") do %>
          <%= link_to "Page admin", "#", :class => "dropdown-toggle", :data => { :toggle => "dropdown" } %>
          <%= content_tag :ul, :class => "dropdown-menu" do %>
            <%= content_tag :li, link_to("All pages", pages_path) %>
            <%= content_tag :li, link_to("New page", new_page_path) %>
          <% end %>
        <% end %>
        <%= content_tag :li, link_to("Users", users_path) %>
      </ul>
    </div>
  </div>
<% end %>
